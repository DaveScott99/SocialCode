import React, { useEffect } from "react";
import { useNavigate } from "react-router";

export default function UnauthorizedRequest() {
    return (
       <h1>Faça login ou Registre-se primeiro</h1>
    );
};